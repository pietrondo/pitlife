🎯 What: Added explicit error logging to an empty catch block when loading the `i18n.json` config file in `Localization/I18n.cs`.
💡 Why: Swallowing exceptions silently makes debugging issues extremely difficult. By properly logging the exception, we improve the maintainability and debuggability of the localization system while keeping the original fallback behavior intact.
✅ Verification: Ran `dotnet build` and `dotnet test`. Verified that all tests passed (the failing tests `AllBuiltinSpecies_HaveTexture` and `Creature_DesertAdaptation_ReducesEnergyDrainAndSpeedPenalty` are pre-existing issues on `master`).
✨ Result: If `i18n.json` fails to load or parse, the error will now be surfaced in the application logs, allowing developers to identify issues faster.
