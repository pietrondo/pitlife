🎯 **What:** Extracted the monolithic `DrawClimateDashboard` method in `UI/InGameUi.cs` into four smaller helper methods (`DrawClimateGlobalData`, `DrawClimateLocalData`, `DrawClimatePopulationData`, and `DrawClimateEventsData`).
💡 **Why:** The original method was overly long and violated the single responsibility principle. This refactor dramatically improves readability and maintainability by isolating global metrics, local tile metrics, population statistics, and event logging into focused blocks.
✅ **Verification:**
- Verified source code changes using `git diff` to ensure no functional behavior was lost.
- Verified compilation and test suite passing (`DOTNET_ROLL_FORWARD=Major dotnet test tests/PitLife.Tests`).
- Brainstorming artifact recorded in `docs/decisions/`.
✨ **Result:** The `InGameUi.cs` codebase is significantly easier to understand and maintain without changing UI functionality.
